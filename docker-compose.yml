version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    volumes:
      - .:/app
      - node_modules:/app/node_modules      # ← preserve install
    restart: always

volumes:
  node_modules: {}